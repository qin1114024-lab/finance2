
name: Deploy WealthWisdom to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 📥
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          # 移除了 cache: 'npm'，因為專案中沒有 lock 檔案

      - name: Build Project 🏗️
        run: |
          # 建立發布資料夾
          mkdir -p dist
          
          # 複製根目錄檔案
          cp index.html dist/
          cp index.tsx dist/
          cp types.ts dist/
          cp constants.tsx dist/
          cp geminiService.ts dist/
          cp metadata.json dist/
          cp app-blueprint.yaml dist/
          
          # 遞迴複製 pages 資料夾
          if [ -d "pages" ]; then
            cp -r pages dist/
          fi

      - name: Inject API Key 🔐
        run: |
          # 將 GitHub Secrets 中的 API_KEY 注入到 geminiService.ts
          # 我們將 process.env.API_KEY 替換為實際的字串值，以便在瀏覽器中直接執行
          if [ -f "dist/geminiService.ts" ]; then
            sed -i "s/process.env.API_KEY/\"${{ secrets.API_KEY }}\"/g" dist/geminiService.ts
          fi

      - name: Deploy to GitHub Pages 🚀
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          force_orphan: true
          publish_branch: gh-pages
